import { CapriShell } from '@/components/capri/capri-shell';

export default function CapriIntegrationsPage() {
  return (
    <CapriShell title="Integrations">
      <div className="capri-card p-12 text-center text-sm" style={{ color: 'var(--capri-on-surface-variant)' }}>
        Meta WhatsApp, CRM webhooks, and partner APIs — demo placeholder.
      </div>
    </CapriShell>
  );
}
